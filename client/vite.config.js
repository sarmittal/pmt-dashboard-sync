import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    // In dev mode, proxy /api/* to the local Express backend
    proxy: {
      "/api": {
        target: "http://localhost:3001",
        changeOrigin: true,
      },
    },
  },
  build: {
    outDir: "dist",
    // Keep chunk size warnings quiet for the large dashboard bundle
    chunkSizeWarningLimit: 1500,
  },
});
